# .env.example
# Copy this file to .env and fill in the values

# application config
PROJECT_NAME="PPE Recommendation Engine API"
VERSION="0.1.0"

# database config (to be used later when connecting to PostgreSQL)
# DATABASE_URL="postgresql://user:password@localhost:5432/risk_assessment_db"

# cors config
BACKEND_CORS_ORIGINS='["http://localhost:3000"]'
